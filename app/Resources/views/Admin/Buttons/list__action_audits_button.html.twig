{% if admin.isGranted('EDIT', object) and admin.hasRoute('audits') %}
    <a href="{{ admin.generateObjectUrl('audits', object) }}" class="btn btn-sm btn-default edit_link" title="Inspeccions"><i class="fa fa-list-alt"></i></a>
{% endif %}
